# D-PlaneOS v2.0.0 — .gitignore

# Go build artifacts
build/dplaned
*.exe
*.test
*.out

# Go module cache (vendor is committed)
# daemon/vendor/ is committed for offline builds

# SQLite runtime databases
*.db
*.db-wal
*.db-shm
*.db.backup

# OS files
.DS_Store
Thumbs.db
desktop.ini

# IDE / Editor
.idea/
.vscode/
*.swp
*.swo
*~
.project
.settings/

# Temporary files
tmp/
*.tmp
*.log

# Legacy v1.x packages (too large for git)
legacy/offline-packages/
legacy/backend/
legacy/frontend-built/

# Node (not used in v2.0.0, but just in case)
node_modules/
